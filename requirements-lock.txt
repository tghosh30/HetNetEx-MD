# Exact versions used to produce the published benchmark numbers.
# Reproducing the paper's tables requires these; ordinary use does not.
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
matplotlib==3.10.7
statsmodels==0.14.6
